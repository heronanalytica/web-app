export const enum ROUTES {
  HOMEPAGE = "/",
  PRICING = "/pricing",
  CONTACT = "/contact",
  LOGIN = "/login",

  APP_HOMEPAGE = "/app",
  APP_CAMPAIGN_NEW = "/app/campaign/new",

  ADMIN_HOMEPAGE = "/admin",
}
